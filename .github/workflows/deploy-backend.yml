name: Deploy to Render

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Render Deploy
        run: |
          # Render auto-deploys from GitHub when connected
          # This step just logs the deployment trigger
          echo "🚀 Push to main detected - Render will auto-deploy"
          echo "📍 Check deployment at: https://dashboard.render.com"
          echo "📝 Commit SHA: ${{ github.sha }}"